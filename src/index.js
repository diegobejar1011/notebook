import React from 'react';
import ReactDOM from 'react-dom/client';
import {HomeNotes} from "../src/templates/HomeNotes";

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(<HomeNotes />);